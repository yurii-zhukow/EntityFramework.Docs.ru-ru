---
title: "Глобальный запрос фильтры - EF Core"
author: anpete
ms.author: anpete
ms.date: 11/03/2017
ms.technology: entity-framework-core
uid: core/querying/filters
ms.openlocfilehash: 4e3c3c99d155f69e00fed99c415f519808ea1a68
ms.sourcegitcommit: 6e379265e4f087fb7cf180c824722c81750554dc
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 11/15/2017
---
# <a name="global-query-filters"></a>Глобальный запрос фильтры

Глобальный запрос фильтры являются предикатов запросов LINQ (логическое выражение, обычно передается LINQ *где* оператора запроса) применяется к типам сущностей в модели метаданных (обычно в *OnModelCreating*). Такие фильтры автоматически применяются к любой запросы LINQ, связанных с этими типами сущностей, включая ссылки на свойства типов сущностей адресуемая косвенно, например, с помощью Include или прямой навигации. Приведены некоторые распространенные приложения этого компонента.

* **Мягкая удалить** -определяет тип сущности *IsDeleted* свойство.
* **Многопользовательские приложения** -определяет тип сущности *TenantId* свойство.

## <a name="example"></a>Пример

В следующем примере показано, как использовать глобальные фильтры запроса для реализации поведения soft-delete и многопользовательский режим запросов в модели простого ведения блогов.

> [!TIP]
> Можно просмотреть в этой статье [пример](https://github.com/aspnet/EntityFrameworkCore/tree/dev/samples/QueryFilters) на GitHub.

Во-первых определите сущности:

[!code-csharp[Main](../../../efcore-dev/samples/QueryFilters/Program.cs#Entities)]

Обратите внимание, объявление __tenantId_ на _блог_ сущности. Это будет использоваться для присоединения к ним каждый экземпляр блог конкретных клиентов. Также определяется _IsDeleted_ свойство _Post_ типа сущности. Этот параметр используется этот параметр, чтобы следить, следует ли _Post_ экземпляр был «удаляется». Т. е. Экземпляр помечен как удаленный withouth физическое удаление базовых данных.

Настройте фильтров запроса в _OnModelCreating_ с помощью ```HasQueryFilter``` API.

[!code-csharp[Main](../../../efcore-dev/samples/QueryFilters/Program.cs#Configuration)]

Предиката выражения, передаваемые _HasQueryFilter_ вызовы теперь автоматически применяются к любой запросы LINQ для этих типов.

> [!TIP]
> Обратите внимание на использование поле уровня экземпляра DbContext: ```_tenantId``` используется для задания текущего клиента. Фильтры на уровне модели будут использовать значение из экземпляра правильный контекст. Т. е. Экземпляр, выполняющего запрос.

## <a name="disabling-filters"></a>Отключение фильтров

Фильтры могут быть отключены для отдельных запросов LINQ с помощью ```IgnoreQueryFilters()``` оператор.

[!code-csharp[Main](../../../efcore-dev/samples/QueryFilters/Program.cs#IgnoreFilters)]

## <a name="limitations"></a>Ограничения

Глобальный запрос фильтров имеют следующие ограничения:

* Фильтры не может содержать ссылки на свойства навигации.
* Фильтры могут быть определены только для корневого типа сущности в иерархии наследования.
